Commands to push to GitHub:

git add .
git commit -m "Add a message"
git push origin main

Command to run locally:

python3 -m http.server 5500

Directions:

Go to Scan page
Scan QR code
It will update the corresponding machine
To reset the machine, either press the reset button or rescan the same QR code